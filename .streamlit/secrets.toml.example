# Optional Streamlit secrets (not required for Schema Compare).
# GitLab URL/project are hardcoded; enter your PAT on the Schema Compare page.

# Example for other future secrets:
# [my_service]
# api_key = "your-key-here"
