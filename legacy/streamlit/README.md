Legacy Streamlit multipage app. Run from repo root:

```bash
streamlit run legacy/streamlit/app.py
```

Or `make legacy-streamlit` after activating the project venv.

`PYTHONPATH` must include the repo root so `db2_explorer` imports resolve.
